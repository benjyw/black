# A canonical location for importing version (e.g., `from black_version import version`)
# that can trigger generation of the underlying _black_version.py as needed.
from _black_version import version
